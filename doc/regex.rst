.. _regular_expressions:

Regular expressions
===================


Regular expressions are string
expressions that match particular strings using wild cards. Regular
expressions are very flexible and powerful, but because of that,
rather cryptic for those who are not used to them. There a
